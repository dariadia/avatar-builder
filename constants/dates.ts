import { DateOption } from 'types/Date'

export const DATE_OPTIONS: DateOption = {
  DATE_MONTH_YEAR: { year: 'numeric', month: 'long', day: 'numeric' },
  MONTH_YEAR: { year: 'numeric', month: 'long' },
}
